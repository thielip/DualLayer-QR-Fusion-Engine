# System packages for Streamlit Community Cloud (opencv-python-headless needs none).
# Keep this file so Cloud detects a Linux-friendly package list if you later switch
# back to opencv-python and need: libgl1
